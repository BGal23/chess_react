// setPawn((prevState) => {
//   const updatedBlackPlace = [...prevState.black.place];
//   const move = id.split("");
//   move.splice(1, 1, (Number(id[1]) + 1).toString());
//   const optionalMove = move.join("");

//   updatedBlackPlace.splice(index, 1, optionalMove);

//   return {
//     ...prevState,
//     black: {
//       ...prevState.black,
//       place: updatedBlackPlace,
//     },
//   };
// });

/////////////////

// const id = event.target.parentElement.id;
// let index;

//const test = document.getElementById("E5");

//test.style.backgroundColor = "rgba(0, 128, 0, 0.7)";
